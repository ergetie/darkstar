[project]
name = "darkstar"
version = "2.5.11-beta"
requires-python = ">=3.12"

[tool.ruff]
# Same line length as previous Black config
line-length = 100
target-version = "py312"

# Exclude same directories as before
exclude = [
    ".git",
    ".venv",
    "venv",
    "__pycache__",
    "static",
    "templates",
    "docs",
    "reference",
    "backend/static",  # Built frontend assets
    "tests",  # Test files use pytest patterns not compatible with strict pyright
    "typings",  # Hand-written .pyi stubs for untyped deps; pyright stubPath, not source
]

[tool.ruff.lint]
# Enable comprehensive rule sets
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # Pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
    "SIM",    # flake8-simplify
    "T201",   # flake8-print
    "TCH",    # flake8-type-checking
    "PTH",    # flake8-use-pathlib
    "RUF",    # Ruff-specific rules
]

# Rules to ignore (with justification)
ignore = [
    "E501",   # Line too long - handled by formatter
    "B008",   # Function call in default argument (common FastAPI pattern)
]

# Allow autofix for all enabled rules
fixable = ["ALL"]
unfixable = []

[tool.ruff.lint.per-file-ignores]
"bin/**/*.py" = ["T201"]
"backend/learning/reflex.py" = ["T201"]
"ml/data_activator.py" = ["T201"]
"ml/evaluate.py" = ["T201"]
"ml/forward.py" = ["T201"]
"ml/pipeline.py" = ["T201"]
"ml/price_train.py" = ["T201"]
"ml/train.py" = ["T201"]
"ml/training_orchestrator.py" = ["T201"]
"scripts/**/*.py" = ["T201"]

[tool.ruff.lint.isort]
# Import sorting compatible with previous conventions
known-first-party = ["backend", "ml"]
force-single-line = false
combine-as-imports = true

[tool.ruff.format]
# Black-compatible formatting
quote-style = "double"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "strict"
stubPath = "typings"

# Include all Python source directories
include = [
    "backend",
    "ml",
    "bin",
    "planner",    # Core planning engine
    "executor",   # Executor engine
    "*.py",       # Root level Python files
]

# Exclude non-source directories
exclude = [
    ".git",
    ".venv",
    "venv",
    "__pycache__",
    "reference",
    "scripts",    # Manual tests, benchmarks, debugging tools
    "typings",    # Third-party type stubs creating false positives
    "tests/recorder/test_recorder_inversion.py", # Mocks with untyped signatures
    "tests/weather/test_weather_interpolation.py", # Mocks with untyped signatures
]

# Report settings for strict mode
reportMissingImports = true
reportMissingTypeStubs = "warning"
reportUnusedImport = true
reportUnusedVariable = true
reportUnusedClass = true

[tool.black]
line-length = 100
