# Darkstar Secrets Configuration
# Copy to secrets.yaml and fill in your values

# Home Assistant Connection
home_assistant:
  url: "http://your-homeassistant:8123"
  token: "your-long-lived-access-token"

# Notification Fallbacks
notifications:
  discord_webhook_url: ""  # Optional: Discord webhook for fallback alerts

# Optional: MariaDB for external logging
# mariadb:
#   host: "localhost"
#   port: 3306
#   database: "darkstar"
#   user: "darkstar"
#   password: "your-password"

# Optional: OpenRouter API key for AI advisor
# openrouter_api_key: "sk-or-v1-..."