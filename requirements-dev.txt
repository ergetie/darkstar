# Testing
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0  # Test coverage tracking (REVIEW-2026-01-04)

# Code Quality (Linting, Formatting, Type Checking)
# Pinned exactly so CI and local resolve the SAME version. Loose pins let CI
# install a newer pyright whose stricter strict-mode inference fails checks that
# pass locally. Bump these deliberately (and re-run the local CI gate) to upgrade.
ruff==0.15.22
pyright==1.1.411

# Type Stubs
pandas-stubs==3.0.3.260530
types-requests==2.33.0.20260712
types-PyYAML==6.0.12.20260518
types-python-dateutil==2.9.0.20260716

# Benchmarking & DX
psutil==7.2.2
rich==15.0.0
