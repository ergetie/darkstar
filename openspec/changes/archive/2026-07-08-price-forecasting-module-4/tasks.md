## 1. Config: goal-based per-charger schema (retire penalty levels)

- [x] 1.1 Add per-charger fields to `EVChargerDeviceConfig` (`executor/config.py`): `target_soc_percent` (int, default 80), `ready_by` (str `HH:MM`), `repeat` (enum `daily`|`weekdays`|`weekends`|`every_n_days`|`none`, default `daily`), `n_days` (int, used when `repeat: every_n_days`), `ready_by_date` (date string, used when `repeat: none`), `keep_on_after_target` (bool, default false). **No `charge_priority` field** — surplus ordering is owned by the existing `excess_pv.priority[]` (`excess-pv-priority-dispatch`); battery is implicitly first via `soc_threshold_percent`.
- [x] 1.2 Accept `departure_time` as a deprecated alias for `ready_by` (log a one-time deprecation warning, copy value forward). Keep the existing YAML 1.2 string-parsing / sexagesimal-int handling for the time value.
- [x] 1.3 Retire `penalty_levels`: it is **not** a dataclass field on `EVChargerDeviceConfig` — it is read as a raw YAML key only at `adapter.py:147-155`. Migration = delete that one comprehension plus the stale comment at `config.default.yaml:110`. If `penalty_levels` is present in a user's config, ignore it for scheduling, log a deprecation warning, and auto-migrate to `target_soc_percent` = the highest configured `max_soc` (so existing users keep an equivalent target).
- [x] 1.4 Update `config.yaml` + `config.default.yaml`: replace the `penalty_levels` example with a default goal (`target_soc_percent: 80`, `ready_by: "07:00"`, `repeat: daily`) and inline comments. Ensure a configured charger charges out of the box.
- [x] 1.5 Unit tests (`tests/ev/test_ev_config.py`): goal fields parse; `departure_time` alias maps to `ready_by` + warns; `penalty_levels` present → ignored + migrated + warns; `repeat: none` requires `ready_by_date`; invalid/missing time handled.

## 2. Pipeline: resolve ready-by + required energy

- [x] 2.1 Add `_resolve_ready_by(charger, now)` in `planner/pipeline.py`: from `ready_by` + `repeat` (or `ready_by_date` for `none`) compute the *next* ready-by datetime (timezone-aware). Daily = next occurrence of the time; weekdays/weekends/every_n_days = next matching day; none = the specific date/time (inert once past).
- [x] 2.2 Add `_calculate_required_kwh(charger, db_path)`: `max(0, (target_soc_percent − current_soc_percent)/100 · battery_capacity_kwh)` minus EV energy already delivered this charging cycle (from `slot_observations` since last plug-in). Clamp ≥ 0.
- [x] 2.3 In the EV section of `pipeline.py` (now at `~739-796`, not the `~536-593` of earlier drafts), attach `required_kwh`, resolved `deadline`, and `keep_on_after_target` to each plugged charger's state dict. Skip unplugged chargers (no requirement). **No `charge_priority` is attached** — it does not exist in this change.

## 3. MultiDayPlanner: automatic spreading (forecast-gated, graceful fallback)

- [x] 3.1 Create `planner/strategy/multi_day_planner.py` with `MultiDayPlanner.compute_quota(remaining_kwh, deadline, daily_prices, max_daily_kwh, min_daily_fraction=0.1) -> dict[date, float]`. Inverse-price weighting: `weight[day] = 1/avg_price[day]`. Keep it **load-agnostic** (no EV-specific params).
- [x] 3.2 Guardrails: min-daily-fraction floor on every non-final day (then redistribute), per-day power cap (`max_power_kw · available_hours`) with redistribution, single-day-remaining = allocate all, zero/negative remaining = zeros, equal prices = equal split, partial/missing prices = fill with average of known days.
- [x] 3.3 Invoke automatically in `pipeline.py`: **only** when the resolved deadline is more than one day out **and** a 7-day forecast exists. Reuse the existing `fetch_price_floor_inputs` (`planner/pipeline.py:48`, already shipped in Module 1/3) — it returns `upcoming_daily_avg_spots: dict[int, float]` (D+1..D+7 → daily avg spot_p50); pass that dict as `daily_prices` to `MultiDayPlanner.compute_quota` (keyed by days-ahead offset, not date — convert as needed). Set today's quota as `daily_quota_kwh` on the charger state. When the deadline is near OR no forecast exists (empty dict), set `daily_quota_kwh = None` (no spreading; Kepler uses real day-ahead prices). **Do not gate the core feature on `price_forecast.enabled` — only the spreading.**
- [x] 3.4 Unit tests (`tests/planner/test_multi_day_planner.py`): inverse-price allocation, min-fraction enforcement, power-cap redistribution, single day, zero remaining, equal prices, partial price data.

## 4. Solver: requirement + self-consumption priority (remove incentive buckets)

- [x] 4.1 Add fields to `EVChargerInput` in **`planner/solver/types.py:33-48`** (NOT `planner/inputs/types.py`, which has no EV types): `required_kwh: float | None`, `deadline: datetime | None`, `daily_quota_kwh: float | None`. Remove `incentive_buckets`. **No `charge_priority` field.**
- [x] 4.2 In `adapter.py`, populate the new fields from charger state; delete the `penalty_levels → IncentiveBucket` mapping.
- [x] 4.3 In `kepler.py`, **remove** the incentive-bucket variables/constraints (now at `:124-149` and `:233-264`, not `:203-234`) and the `Σ ev_bucket_charged · value_sek` reward (now at `:768-777`, not `:642-651`).
- [x] 4.4 Add the soft target-by-time requirement per charger: `Σ ev_energy[d][t] (t ≤ deadline) + shortfall[d] >= required_kwh`; add `shortfall[d] · ev_shortfall_penalty_sek_per_kwh` to the objective. Define module constant `EV_SHORTFALL_PENALTY_DEFAULT = 50.0` in `kepler.py`; add field `ev_shortfall_penalty_sek_per_kwh: float = EV_SHORTFALL_PENALTY_DEFAULT` to `KeplerConfig` (`planner/solver/types.py`); wire it through `adapter.py` from `planner_config.get("kepler", {}).get("ev_shortfall_penalty_sek_per_kwh", EV_SHORTFALL_PENALTY_DEFAULT)` (same pattern as `curtailment_penalty_sek` at `adapter.py:491-493`); document it in `config.default.yaml` under the `kepler:` block. Applies to **both binary and current-type** plugged chargers with a goal — `ev_charge[d][t]` / `ev_energy[d][t]` are created for every plugged charger regardless of `control_type`.
- [x] 4.5 **(Removed)** No excess-PV self-consumption preference term is added. Surplus routing is owned by `excess_pv.priority[]` (`excess-pv-priority-dispatch`); battery is implicitly first via `soc_threshold_percent`, and EV surplus only fires for `type: current` chargers listed in the priority list. **Do not implement this task.** (Verified: no such term exists.)
- [x] 4.6 Apply `daily_quota_kwh` (when not None) as an upper bound on today's EV energy: `Σ ev_energy[d][t] <= daily_quota_kwh`.
- [x] 4.7 Keep the deadline as a time-window constraint (`ev_energy[d][t] == 0` for slots ending after the deadline).
- [x] 4.8 Solver tests (`tests/planner/test_kepler_ev_target.py`): configured current-type charger (listed in `excess_pv.priority[]`) charges from surplus PV instead of exporting; reaches `target_soc` by deadline when feasible; reports shortfall (stays feasible) when not; daily quota caps today's energy; no incentive-bucket code path remains; shortfall penalty defaults to 50.0 and is overridden by `kepler.ev_shortfall_penalty_sek_per_kwh` when set. **Add:** a binary charger in `excess_pv.priority[]` is silently dropped (no `ev_surplus_kw` variable created) but still receives scheduled charging per its soft deadline-target. *(The `charge_priority` test case is removed — the field does not exist.)*

## 5. Read-only EV state API

- [x] 5.1 Create `backend/api/routers/ev.py` with `GET /api/ev/chargers`; register in `backend/api/app.py`.
- [x] 5.2 Persist transient state to `data/ev_multi_day_state.json` at the end of each pipeline run: per charger `id`, goal (`target_soc_percent`, `ready_by`, `repeat`, resolved `deadline`), `required_kwh`, `delivered_kwh`, `remaining_kwh`, `daily_quota_kwh`, `quota_schedule` (when spreading), `keep_on_after_target`, `status`, `last_updated`. **No `charge_priority` is persisted** (the field does not exist).
- [x] 5.3 Endpoint merges state file with live HA sensor data (`plugged_in`, `soc_percent`, `power_kw`, fetched on request, reusing the system-status HA pattern in `backend/api/routers/system.py`). `status ∈ {on_track, behind, complete, idle}` computed from remaining vs deliverable-by-deadline. **No `charge_priority` in the response.**
- [x] 5.4 Tests (`tests/backend/test_ev_api.py`): returns all configured chargers with goal + progress + status; spreading charger includes `quota_schedule`; non-spreading charger has null quota fields; missing/stale state file → `idle` + live sensors still populated.

## 6. End-to-end + migration

- [x] 6.1 E2E test (`tests/planner/test_e2e_ev_target.py`): charger at 30% SoC, `target 80%` by tomorrow 07:00, midday surplus PV → schedule charges the EV from surplus (not export) and reaches 80% by the deadline using cheapest slots.
- [x] 6.2 E2E multi-day: `ready_by` 3 days out + forecast with a cheap middle day → more energy allocated to the cheap day, today's quota respected, target met by the deadline.
- [x] 6.3 Migration test: a config still using `penalty_levels` loads with a deprecation warning, migrates to an equivalent `target_soc_percent`, and charges correctly (no incentive-bucket path executed).
- [x] 6.4 Confirm the full suite still passes (1051+ baseline) and no `incentive_bucket` references remain in planner/solver.
