## 0. Orientation (read before starting)

- [x] 0.1 Read `proposal.md`, `design.md`, and all four files under `specs/` in this change directory. Every task below implements a scenario from those specs.
- [x] 0.2 Confirm the working branch is clean (`git status`) and tests pass at baseline before editing: run `pytest tests/ -q` and note the pass count.
- [x] 0.3 Line numbers in these tasks are anchors from a code map and may have drifted a few lines — always locate the named **function/symbol**, not the raw line number.

## 1. Fix #24 — notify the operator on repeated command failures (backend, top priority)

- [x] 1.1 In `executor/engine.py`, in `ExecutorEngine.__init__` (near the EV failure state at ~`engine.py:207-208`, the `self._ev_zero_power_ticks` / `self._ev_failure_notified` lines), add two new instance fields: `self._action_fail_counts: dict[str, int] = {}` and `self._action_fail_notified: set[str] = set()`.
- [x] 1.2 Add a module-level constant near the top of `executor/engine.py` (with the other constants): `ACTION_FAILURE_NOTIFY_STREAK = 3` (number of consecutive failing ticks for one action type before notifying). Add a comment: "consecutive-tick threshold before a command-failure push; deterministic rejections, so lower than the EV zero-power threshold (5)".
- [x] 1.3 In the failed-result handling loop inside the tick coroutine (`_run_tick`, ~`engine.py:1444-1460`, the loop that filters `not r.success and not r.skipped`, appends to `self.recent_errors`, and emits the `executor_error` WebSocket event), add streak tracking: for each failing result, increment `self._action_fail_counts[r.action_type]` (default 0). When a count reaches `ACTION_FAILURE_NOTIFY_STREAK` and `r.action_type not in self._action_fail_notified`, build a message like `f"Command failure: {r.action_type} failed {count}x — {r.message}"`, call `if self.dispatcher: await self.dispatcher.notify_error(msg)`, then add `r.action_type` to `self._action_fail_notified`. (Mirror the EV pattern at `engine.py:1343-1352`.)
- [x] 1.4 In the same tick, reset the streak for action types that did NOT fail this tick: for every result with `r.success or r.skipped`, `self._action_fail_counts.pop(r.action_type, None)` and `self._action_fail_notified.discard(r.action_type)`. (Mirror the EV reset at `engine.py:1368-1369`.) Place this so it runs whether or not any failure occurred this tick.
- [x] 1.5 Confirm no change to `dispatcher.notify_error` is needed — it already gates on `notifications.on_error` (`executor/actions.py:1229-1235`). Do NOT add a second gate.
- [x] 1.6 Add a test in `tests/executor/` (new file `test_command_failure_notification.py` or the nearest existing engine test module): (a) a fake dispatcher records `notify_error` calls; (b) drive `ACTION_FAILURE_NOTIFY_STREAK` consecutive ticks where one action type fails → assert exactly one `notify_error` call; (c) one more failing tick → assert still one call (dedup holds); (d) a successful tick for that type then a new failure streak → assert a second call fires; (e) with `notifications.on_error` false → assert zero calls.

## 2. Fix #3 — populate `execution_log.error_message` (backend, same file as #1)

- [x] 2.1 In `executor/engine.py`, in `_create_execution_record` (~`engine.py:1819-1885`, where the `action_results` list of dicts is built at ~`1865-1879` and the `ExecutionRecord(...)` is returned), when the aggregate `success` argument is `False`, build a summary string from the failing results: join `f"{r.action_type}: {r.message}"` for each result with `not r.success and not r.skipped`, separated by `"; "`, and truncate to 500 chars. When `success` is `True`, use `None`.
- [x] 2.2 Pass that summary as `error_message=<summary>` into the `ExecutionRecord(...)` constructor call (the dataclass already has the field, `executor/history.py:66`; the DB writer already persists it, `history.py:133`). No schema or migration change.
- [x] 2.3 Add/extend a test in `tests/executor/`: build a record from a mix of failed + successful `ActionResult`s and assert the persisted `ExecutionLog.error_message` is non-null and names the failed action type; build a fully-successful record and assert `error_message is None`.

## 3. Fix #18 — dashboard connection resilience (frontend)

- [x] 3.1 In `frontend/src/lib/socket.ts`, in the `Manager` options (~`socket.ts:58-66`), change `reconnectionAttempts: 10` to `reconnectionAttempts: Infinity`. Leave `reconnectionDelay: 1000` and `reconnectionDelayMax: 5000` unchanged (backoff stays bounded).
- [x] 3.2 In `frontend/src/lib/socket.ts`, add a module-level connection-state store the rest of the app can subscribe to. Simplest approach matching this codebase (plain React, no zustand): keep a `let connected = false` plus a `Set<(v:boolean)=>void>` of listeners, and export `subscribeConnection(listener)` / `getConnectionState()`. Update `connected` and notify listeners from the `socket.on('connect', ...)` (true) and `socket.on('disconnect', ...)` (false) handlers (currently console-only at ~`socket.ts:113-119`).
- [x] 3.3 In `frontend/src/lib/hooks.ts` (where `useSocket` already wraps `getSocket`), add a companion hook `useSocketStatus(): boolean` that subscribes via `subscribeConnection` in a `useEffect`, returns the current connected boolean as React state, and unsubscribes on cleanup. Mirror the existing `useSocket` structure.
- [x] 3.4 In `frontend/src/pages/Dashboard.tsx`, add refetch-on-reconnect: a `useEffect` that watches `useSocketStatus()` and, when it transitions from disconnected→connected (guard with a `useRef` "wasConnected" flag so it does NOT fire on the very first mount connect, since `fetchAllData` already runs on mount at ~`Dashboard.tsx:442-444`), calls the existing `fetchAllData` (~`Dashboard.tsx:419`).
- [x] 3.5 Add a visible live/stale indicator to the dashboard header. Create/mirror a small badge using `frontend/src/components/ui/Banner.tsx` (`Badge` variants `good`/`bad`): render `useSocketStatus()` as a green "Live" dot when connected and a red "Reconnecting…"/"Stale" dot when disconnected. Place it near the existing header/refresh control (~`Dashboard.tsx:728` area).
- [x] 3.6 Verify with the frontend test/lint setup: `cd frontend && npm run lint` (ESLint + Prettier) passes; if a component test harness exists for hooks, add a minimal test that `useSocketStatus` reflects connect/disconnect. Manually confirm (or note for the operator to confirm) that stopping the backend flips the indicator to stale and restarting it flips back to live and refetches.

## 4. Fix #9 — surface runtime monitors in the UI + drop the dead table

### 4a. Backend: drop `data_quality_daily`

- [x] 4.1 Create a new Alembic migration in `alembic/versions/` (mirror an existing recent file, e.g. `8f2c4d6e9a10_add_openmeteo_pv_forecast_column.py`). Set `down_revision = "8f2c4d6e9a10"` (the current HEAD — verify with `alembic heads` that nothing newer exists). `upgrade()` = `op.drop_table("data_quality_daily")` (guard with an `sa.inspect(conn).has_table(...)` check like the idempotent migrations do). `downgrade()` = recreate the table by copying the `op.create_table("data_quality_daily", ...)` column list from the baseline `alembic/versions/f6c8f45208da_baseline.py` (~lines 101-115). Add a comment noting historical rows are not restored (backfill-era only, superseded by runtime monitors).
- [x] 4.2 Remove the `DataQualityDaily` model class from `backend/learning/models.py` (~lines 283-295).
- [x] 4.3 Grep the repo for `data_quality_daily` and `DataQualityDaily`: `grep -rn "data_quality_daily\|DataQualityDaily" backend/ ml/ alembic/`. The ONLY remaining hits should be the two new migration references (drop + downgrade recreate), the baseline migration, and the descriptive comments in `backend/monitors.py` (~lines 38, 288). Confirm no live writer/reader remains.
- [x] 4.4 Run the migration against a scratch copy of the DB (or a fresh test DB) up and back down: `alembic upgrade head` then `alembic downgrade -1` then `alembic upgrade head` — must succeed both directions without error.

### 4b. Frontend: monitor status panel

- [x] 4.5 In `frontend/src/lib/api.ts`, add a typed client method under the `system` block (near the existing `systemHealth` at ~`api.ts:761-763`): `monitors: () => getJSON<MonitorStatus>('/api/system/monitors')`. Define the `MonitorStatus` response type (near the system type block ~`api.ts:553`) matching `get_status()` shape: `{ running: boolean; healthy: boolean; last_cycle_at: string|null; last_error: string|null; invariants: Record<string, { name: string; status: 'pass'|'violation'|'skipped'; detail: string; evaluated_at: string }>; active_violations: { invariant: string; first_detected_at: string; detail: string }[] }` (source of truth: `backend/monitors.py` `get_status`, ~lines 381-396).
- [x] 4.6 Create a read-only panel component `frontend/src/components/MonitorStatusCard.tsx` mirroring `frontend/src/components/SystemHealthCard.tsx` (fetch on mount + `setInterval` refresh ~60s, loading state, a status→color mapper, `Card` wrapper). Render: overall `running`/`healthy`, each entry in `invariants` with its `status` colored (pass=green, violation=red, skipped=muted) and `detail`, and the `active_violations` list with `invariant` + `first_detected_at` + `detail`. On fetch error, render an error/empty state (do not throw).
- [x] 4.7 Place the panel in Settings→Debug as a new sub-tab. In `frontend/src/pages/Debug.tsx`, `DebugContent` (~line 382) has an internal tab switcher (`activeTab: 'logs' | 'loads'`, buttons ~450-473, render switch ~476-493). Add a third tab `'monitors'` (label "Monitors") that renders `<MonitorStatusCard />`. (Rationale: Debug is advanced-only and the natural diagnostics home; the operator approved Aurora tab OR Settings→Debug — this uses Debug.)
- [x] 4.8 Verify: `cd frontend && npm run lint` passes; load the Debug→Monitors tab against a running backend and confirm the seven invariants render; confirm the panel shows an error state (not a blank crash) when `/api/system/monitors` is unreachable.
- [x] 4.9 Update the OpenAPI check if the repo enforces one for new client methods: run the repo's OpenAPI/type-sync check (the same one CI runs) and regenerate types if required. (No new backend endpoint is added — it already exists — so this is usually a no-op; confirm.)

## 5. Fix #8 — flag-aware ML training + tag the January bad cluster

- [x] 5.1 Define the exclusion semantic. `slot_observations.quality_flags` is a JSON-object string; today it only holds `{"source": "recorder"|"backfill"}` (built in `backend/learning/store.py` ~137-157). The exclude marker is the additional key `"exclude": true` in that same JSON object. Document this in a comment where the flag is built (`store.py`) and where it is filtered (`ml/train.py`). Do NOT overwrite or drop the existing `"source"` key when adding `"exclude"`.
- [x] 5.2 Add the training filter. In `ml/train.py`, `_load_slot_observations` (~lines 111-197) has FOUR query branches (~121-197) that select from `slot_observations o`. Add to EVERY branch's WHERE clause a condition that excludes rows flagged for exclusion, e.g. `AND (o.quality_flags IS NULL OR o.quality_flags NOT LIKE '%"exclude": true%')`. Keep the existing `load_kwh`/`pv_kwh` filters. Ensure the exact JSON substring matches how 5.1 writes it (mind spaces: write and match `"exclude": true` consistently).
- [x] 5.3 Add a test in `tests/ml/`: insert two `slot_observations` rows (one with `quality_flags` containing `"exclude": true`, one without), run `_load_slot_observations`, and assert the excluded row is absent and the unflagged row is present. Add a second assertion that a row with only `{"source":"recorder"}` (no exclude key) is still included.
- [x] 5.4 Write a one-off tagging script `scripts/flag_january_bad_slots.py` (mirror the style of existing `scripts/*.py` diagnostic tools). It SHALL: connect to the learning DB read the target slots, and for each `slot_observations` row in the known-bad window matching the physical-impossibility criterion from finding #8 — `slot_start` in 2026-01-16..2026-01-20 AND `batt_charge_kwh > 0.3` AND `import_kwh < 0.05` AND `pv_kwh < 0.5 * batt_charge_kwh` — merge `"exclude": true` into the existing `quality_flags` JSON (preserve `"source"`), writing back only that column. It SHALL print how many rows it would touch and require an explicit `--apply` flag to write (dry-run by default). It SHALL NOT modify any measurement column.
- [x] 5.5 Test the script logic on a scratch/test DB (seed a couple of matching rows + a non-matching row): dry-run reports the right count and writes nothing; `--apply` sets `"exclude": true` on only the matching rows and leaves `"source"` intact and measurements unchanged.
- [x] 5.6 Do NOT run the tagging script against production — that is the operator's action (document the exact command in the script's `--help` / a comment for the operator to run after deploy). Note in the change that measurements are never altered; only the annotation flag is added.

## 6. Verification (whole change)

- [x] 6.1 Backend lint + types: run `ruff check .`, `ruff format --check .`, and the repo's pyright config (strict) — all clean on changed files.
- [x] 6.2 Backend tests: `pytest tests/ -q` — all pass, including the new tests from 1.6, 2.3, 5.3, 5.5, and the migration round-trip from 4.4.
- [x] 6.3 Frontend: `cd frontend && npm run lint && npm run build` — clean.
- [x] 6.4 Run the repo's OpenAPI/type-sync check (whatever CI runs) — clean.
- [ ] 6.5 Manual smoke (or hand to operator): with the backend running, confirm (a) a forced command failure eventually pushes a notification once, (b) the dashboard indicator flips stale↔live across a backend restart and refetches, (c) Debug→Monitors renders the invariants.
